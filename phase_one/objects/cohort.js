const cohort = {
  name: 'May2022',
  id: 1234,
  students: ['Eliza', 'Jude', 'Will', 'Mike', 'Aimée', 'Henry']
};

console.log(`${cohort.id} - ${cohort.name} - ${cohort.students.length} students in this cohort`);